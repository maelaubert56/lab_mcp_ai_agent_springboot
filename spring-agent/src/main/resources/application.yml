spring:
  application:
    name: agent

server:
  port: 8081

github:
  owner: ${GITHUB_OWNER}
  repo: ${GITHUB_REPO}

google-ai:
  api-key: ${GOOGLE_AI_API_KEY}
  model: gemini-3-flash-preview
  timeout-seconds: 60

mcp:
  base-url: http://localhost:3333
  path: /mcp
