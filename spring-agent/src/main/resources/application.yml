spring:
  application:
    name: agent

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

  timeout-seconds: 60

mcp:
  base-url: ${MCP_BASE_URL:http://localhost:3333}
  path: ${MCP_PATH:/mcp}
