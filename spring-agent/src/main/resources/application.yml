spring:
  application:
    name: agent

server:
  port: 8081

github:
  owner: ${GITHUB_OWNER}
  repo: ${GITHUB_REPO}

google-ai:
  api-key: ${GOOGLE_AI_API_KEY}
  model: gemini-flash-lite-latest
  timeout-seconds: 60

mcp:
  base-url: ${MCP_BASE_URL:http://localhost:3333}
  path: ${MCP_PATH:/mcp}
